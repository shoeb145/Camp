<% layout('layout/boilerplate') %>
    <h1>
        <%= campground.title %>
    </h1>
    <p>
        <%= campground.location %>
    </p>
    <p><img src="<%= campground.image %>" alt="" srcset=""></p>

    <p>
    <form action="/campgrounds/<%= campground._id %>?_method=DELETE" method="post">
        <button type="submit">delete</button>
    </form>
    </p>